services:
  web-server:
    image: nginx:latest
    container_name: nginx
    ports:
      - "10000:80"  # 호스트 80포트와 컨테이너 80포트 연결
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./html:/usr/share/nginx/html
